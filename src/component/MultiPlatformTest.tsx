import React from "react";
import { View, StyleSheet, Text } from "react-native";

type Props = {};

export const MultiPlatformTest = (props: Props) => {
  const {} = props;

  return (
    <View>
      <Text>Web</Text>
    </View>
  );
};
